# Frontend environment — copy to `.env` to override.
# Same-origin: a relative path the Vite dev proxy (and nginx in prod) forwards
# to the backend. Do NOT set an absolute backend URL here.
VITE_API_URL=/api/v1
