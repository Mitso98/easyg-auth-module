# Backend — NestJS + MongoDB auth API

See the [root README](../README.md) for setup, architecture, environment
variables, API docs, testing, and deployment.

Backend-specific conventions (layering, security, database, error handling)
live in [CLAUDE.md](./CLAUDE.md).

```bash
npm install
npm run start:dev   # http://localhost:3000  (needs Mongo + a .env — see root README)
npm test
```
